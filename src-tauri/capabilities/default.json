{
  "identifier": "default",
  "description": "Default permissions for Moscad",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-sidecar",
    {
      "identifier": "shell:allow-sidecar",
      "allow": [{ "sidecar": "sidecar" }]
    }
  ]
}
