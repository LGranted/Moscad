{
  "identifier": "default",
  "description": "Default permissions for Moscad",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-spawn",
    {
      "identifier": "shell:allow-spawn",
      "allow": [{ "sidecar": "sidecar" }]
    }
  ]
}
